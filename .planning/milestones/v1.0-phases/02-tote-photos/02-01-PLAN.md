---
phase: 02-tote-photos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db.ts
  - src/types/index.ts
  - src/lib/photos.ts
  - src/app/api/totes/[id]/photos/route.ts
  - src/app/api/totes/route.ts
  - src/app/api/totes/[id]/route.ts
  - src/app/api/photos/[id]/route.ts
  - src/app/api/photos/[id]/thumbnail/route.ts
  - src/app/api/export/route.ts
  - src/app/api/import/route.ts
autonomous: true
requirements: [PHOTO-01, PHOTO-04, PHOTO-05, PHOTO-06, PHOTO-07]

must_haves:
  truths:
    - "tote_photos table is created on app startup alongside existing tables"
    - "Uploading a photo to a tote via POST /api/totes/:id/photos saves file, thumbnail, and DB record"
    - "Photo serving routes (/api/photos/:id and /api/photos/:id/thumbnail) can serve both item and tote photos via ?source=tote param"
    - "Deleting a photo via DELETE /api/photos/:id?source=tote removes tote photo from DB and disk"
    - "Deleting a tote removes both item photo and tote photo files from disk"
    - "Export ZIP includes tote_photos records in JSON data"
    - "Import restores tote_photos from JSON data (optional field for backward compat)"
  artifacts:
    - path: "src/lib/photos.ts"
      provides: "Shared photo processing utility (processPhotoUpload, getMaxUploadSize, deletePhotoFiles)"
      exports: ["processPhotoUpload", "getMaxUploadSize", "deletePhotoFiles"]
    - path: "src/app/api/totes/[id]/photos/route.ts"
      provides: "POST (upload) and GET (list) for tote photos"
      exports: ["POST", "GET"]
    - path: "src/types/index.ts"
      provides: "TotePhoto interface and PhotoRecord shared type"
      contains: "interface TotePhoto"
  key_links:
    - from: "src/app/api/totes/[id]/photos/route.ts"
      to: "src/lib/photos.ts"
      via: "processPhotoUpload() call"
      pattern: "processPhotoUpload"
    - from: "src/app/api/photos/[id]/route.ts"
      to: "tote_photos table"
      via: "source query param selects table"
      pattern: "source.*tote.*tote_photos"
    - from: "src/app/api/totes/[id]/route.ts"
      to: "tote_photos table"
      via: "cascade delete file cleanup"
      pattern: "tote_photos.*tote_id"
    - from: "src/app/api/export/route.ts"
      to: "tote_photos table"
      via: "SELECT * FROM tote_photos"
      pattern: "tote_photos"
    - from: "src/app/api/import/route.ts"
      to: "tote_photos table"
      via: "INSERT INTO tote_photos"
      pattern: "tote_photos"
---

<objective>
Build the complete backend for tote photos: database table, shared processing utility, tote photo API endpoints, and updates to all existing routes that must handle tote photos (serving, deletion, cascade delete, export, import).

Purpose: This is the foundation for tote photos. No UI work can proceed without the API, types, and shared utility being in place.
Output: Working tote photo upload/list/serve/delete API, updated export/import with tote photos, cascade delete including tote photos.
</objective>

<execution_context>
@/Users/kenmoini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kenmoini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tote-photos/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/types/index.ts:
```typescript
export interface ItemPhoto {
  id: number;
  item_id: number;
  filename: string;
  original_path: string;
  thumbnail_path: string;
  file_size: number;
  mime_type: string;
  created_at: string;
}
```

From src/lib/db.ts:
```typescript
export function getDb(): Database.Database;
export function generateToteId(): string;
export function getUploadDir(): string;
export function getThumbnailDir(): string;
```

From src/lib/magic-bytes.ts:
```typescript
export function detectMimeType(buffer: Buffer): string | null;
export function validateImageBuffer(buffer: Buffer): { valid: boolean; detectedType: string | null; };
```

From src/lib/validation.ts:
```typescript
export const IdParam: z.ZodNumber; // coerce.number().int().positive()
export function safePath(baseDir: string, userInput: string): string | null;
```

From src/app/api/items/[id]/photos/route.ts (upload pattern to extract into shared utility):
- Lines 58-123: MIME pre-filter, size check, buffer read, magic bytes validation, filename generation, file write, sharp thumbnail, DB insert
- Uses: ALLOWED_TYPES, THUMBNAIL_WIDTH=200, THUMBNAIL_HEIGHT=200, crypto.randomBytes(16)

From src/app/api/photos/[id]/route.ts (serving pattern to update):
- GET: Queries `item_photos` table only, serves file with Cache-Control headers
- DELETE: Queries `item_photos` table only, deletes DB record first, then files

From src/app/api/photos/[id]/thumbnail/route.ts:
- GET: Queries `item_photos` table only, serves thumbnail

From src/app/api/totes/[id]/route.ts (cascade delete pattern):
- DELETE: Queries item_photos via JOIN items, deletes tote row (CASCADE), then cleans up files
- Must also query tote_photos directly for tote's own photos

From src/app/api/export/route.ts:
- Queries all tables, builds exportData.data object, archives files from uploads/ and thumbnails/ dirs

From src/app/api/import/route.ts:
- ExportData interface has hardcoded data shape
- validateExportData checks requiredTables array
- Transaction clears and re-inserts all tables
- tote_photos must be OPTIONAL (backward compat with pre-Phase-2 exports)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared photo utility, DB table, types, and tote photo API</name>
  <files>src/lib/photos.ts, src/lib/db.ts, src/types/index.ts, src/app/api/totes/[id]/photos/route.ts</files>
  <action>
**1. Create `src/lib/photos.ts`** -- Extract photo upload processing from `src/app/api/items/[id]/photos/route.ts` into a shared utility:
- `processPhotoUpload(file: File, maxSize: number): Promise<{ data: PhotoUploadResult } | { error: PhotoUploadError }>` -- handles MIME pre-filter (ALLOWED_TYPES = ['image/jpeg', 'image/png', 'image/webp']), size check, buffer read, magic bytes validation via `validateImageBuffer()`, filename generation via `crypto.randomBytes(16).toString('hex')` + extension from detected type, file write to `getUploadDir()`, sharp thumbnail generation (200x200, fit: cover, center, .rotate() for EXIF) to `getThumbnailDir()`, returns `{ filename, originalPath, thumbnailPath, fileSize, mimeType }`.
- `getMaxUploadSize(): number` -- reads from settings table, defaults to 5242880.
- `deletePhotoFiles(originalPath: string, thumbnailPath: string): void` -- resolves paths via `path.resolve(baseDir, path.basename(path))`, validates with startsWith check, unlinkSync with try-catch per file. Uses `getUploadDir()` and `getThumbnailDir()`.
- Import types: `PhotoUploadResult` and `PhotoUploadError` interfaces exported.

**2. Add `tote_photos` table to `src/lib/db.ts`** -- Add to `initializeSchema()` after `item_photos`:
```sql
CREATE TABLE IF NOT EXISTS tote_photos (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  tote_id TEXT NOT NULL,
  filename TEXT NOT NULL,
  original_path TEXT NOT NULL,
  thumbnail_path TEXT NOT NULL,
  file_size INTEGER NOT NULL,
  mime_type TEXT NOT NULL,
  created_at DATETIME DEFAULT (datetime('now')),
  FOREIGN KEY (tote_id) REFERENCES totes(id) ON DELETE CASCADE
);
```

**3. Add types to `src/types/index.ts`**:
- `TotePhoto` interface (mirrors `ItemPhoto` with `tote_id: string` instead of `item_id: number`)
- `PhotoRecord` interface -- shared type for components: `{ id, filename, original_path, thumbnail_path, file_size, mime_type, created_at }` (no entity-specific FK field)
- Update `DashboardData` to add `recent_totes` field: `recent_totes: (Tote & { item_count: number; cover_photo_id: number | null })[]`

**4. Create `src/app/api/totes/[id]/photos/route.ts`**:
- POST: Validate tote ID format (regex `^[a-zA-Z0-9]{6}$`), verify tote exists, check photo count <= 3 from `tote_photos`, parse multipart formData for 'photo' field, call `processPhotoUpload(file, getMaxUploadSize())`, if error return error response, else INSERT into `tote_photos` (tote_id, filename, original_path, thumbnail_path, file_size, mime_type), return 201 with photo record.
- GET: Validate tote ID, query `SELECT * FROM tote_photos WHERE tote_id = ? ORDER BY created_at ASC` (ASC so first = cover photo), return `{ data: photos }`.

**5. Refactor `src/app/api/items/[id]/photos/route.ts`** to use the shared utility:
- Replace inline upload logic (lines 58-123) with a call to `processPhotoUpload(file, getMaxUploadSize())`.
- Keep the item-specific parts: ID validation, item existence check, photo count check, DB INSERT into `item_photos`.
- This proves the shared utility works with the existing, tested item photo flow.
  </action>
  <verify>
    <automated>cd /Users/kenmoini/Development/tote-sonar && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
- src/lib/photos.ts exists with processPhotoUpload, getMaxUploadSize, deletePhotoFiles exports
- tote_photos table added to db.ts initializeSchema
- TotePhoto, PhotoRecord types added to types/index.ts
- DashboardData type includes recent_totes field
- POST /api/totes/:id/photos creates tote photo with shared utility
- GET /api/totes/:id/photos lists tote photos ordered by created_at ASC
- Item photo POST route refactored to use shared processPhotoUpload (same behavior, less code)
- TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Update photo serving routes and cascade delete for tote photos</name>
  <files>src/app/api/photos/[id]/route.ts, src/app/api/photos/[id]/thumbnail/route.ts, src/app/api/totes/[id]/route.ts</files>
  <action>
**1. Update `src/app/api/photos/[id]/route.ts`** (GET and DELETE):
- Read `source` query param: `const source = request.nextUrl.searchParams.get('source');`
- Determine table: `const table = source === 'tote' ? 'tote_photos' : 'item_photos';`
- GET: Replace hardcoded `item_photos` query with parameterized table name: `db.prepare(\`SELECT * FROM ${table} WHERE id = ?\`).get(photoId)`. Rest unchanged.
- DELETE: Same table selection. Delete DB record from the selected table. Use `deletePhotoFiles()` from `src/lib/photos.ts` for file cleanup instead of inline try-catch blocks. This satisfies PHOTO-04.

**2. Update `src/app/api/photos/[id]/thumbnail/route.ts`** (GET):
- Same `source` query param pattern as above. Replace `item_photos` with parameterized table.

**3. Update `src/app/api/totes/[id]/route.ts`** (DELETE handler only):
- BEFORE deleting the tote row, also query tote photos: `SELECT original_path, thumbnail_path FROM tote_photos WHERE tote_id = ?`
- Combine with existing item photos query results.
- After CASCADE delete of tote row, clean up ALL photo files (both item and tote photos) using `deletePhotoFiles()` from `src/lib/photos.ts`.
- Import `deletePhotoFiles` from `@/lib/photos`. This satisfies PHOTO-05.
  </action>
  <verify>
    <automated>cd /Users/kenmoini/Development/tote-sonar && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
- GET /api/photos/:id?source=tote serves tote photo from tote_photos table
- GET /api/photos/:id/thumbnail?source=tote serves tote photo thumbnail
- DELETE /api/photos/:id?source=tote deletes tote photo from DB and disk
- DELETE /api/totes/:id cleans up both item and tote photo files
- TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Update totes list query, export, and import for tote photos</name>
  <files>src/app/api/totes/route.ts, src/app/api/export/route.ts, src/app/api/import/route.ts</files>
  <action>
**1. Update `src/app/api/totes/route.ts`** (GET handler):
- Add cover_photo_id subquery to the totes list query:
```sql
SELECT t.*, COUNT(i.id) as item_count,
  (SELECT tp.id FROM tote_photos tp WHERE tp.tote_id = t.id ORDER BY tp.created_at ASC LIMIT 1) as cover_photo_id
FROM totes t
LEFT JOIN items i ON i.tote_id = t.id
GROUP BY t.id
ORDER BY t.${safeSortBy} ${safeSortOrder}
```
- This satisfies PHOTO-02 backend requirement (frontend will consume cover_photo_id in Plan 03).

**2. Update `src/app/api/export/route.ts`**:
- Add `const totePhotos = db.prepare('SELECT * FROM tote_photos ORDER BY created_at').all();`
- Add `tote_photos: totePhotos` to the `exportData.data` object. This satisfies PHOTO-06.
- Photo files are already exported from uploads/ and thumbnails/ dirs (all files), so no additional file handling needed.

**3. Update `src/app/api/import/route.ts`**:
- Update the `ExportData` interface to add optional `tote_photos?: Array<Record<string, unknown>>` to the data shape.
- Do NOT add 'tote_photos' to the `requiredTables` array -- keep it optional for backward compatibility with pre-Phase-2 exports (Pitfall 5 from research).
- Add `DELETE FROM tote_photos` to the clear step (between `DELETE FROM item_photos` and `DELETE FROM items`).
- Add tote_photos INSERT block after item_photos: `if (data.tote_photos && data.tote_photos.length > 0) { ... }` using prepared statement with all columns.
- Update the import summary to include tote_photos count: `tote_photos: (data.tote_photos || []).length`.
- This satisfies PHOTO-07.
  </action>
  <verify>
    <automated>cd /Users/kenmoini/Development/tote-sonar && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
- GET /api/totes includes cover_photo_id in response
- Export ZIP JSON includes tote_photos array
- Import handles tote_photos (optional for backward compat, clears and re-inserts)
- TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit`
2. App starts without crash: `npm run dev` (verify tote_photos table created in console output)
3. Tote photo upload: `curl -X POST -F "photo=@test.jpg" http://localhost:3000/api/totes/TESTID/photos` returns 201
4. Tote photo list: `curl http://localhost:3000/api/totes/TESTID/photos` returns photos array
5. Photo serving: `curl http://localhost:3000/api/photos/1?source=tote` serves photo
6. Totes list includes cover_photo_id: `curl http://localhost:3000/api/totes` response has cover_photo_id field
7. Export includes tote_photos: Download export, inspect JSON for tote_photos array
</verification>

<success_criteria>
- Shared photo utility DRYs up upload logic between item and tote photo routes
- Tote photos can be uploaded, listed, served, and deleted via API
- Cascade delete cleans up tote photo files alongside item photo files
- Export/import round-trip preserves tote photos
- Backward compatible: importing a pre-Phase-2 export still works
- All TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-tote-photos/02-01-SUMMARY.md`
</output>
