---
phase: 02-tote-photos
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/app/totes/[id]/page.tsx
  - src/app/totes/page.tsx
  - src/app/api/dashboard/route.ts
  - src/app/page.tsx
autonomous: false
requirements: [PHOTO-01, PHOTO-02, PHOTO-03]

must_haves:
  truths:
    - "Tote detail page shows photo gallery above items list with upload capability"
    - "Tote list page shows cover thumbnail (first uploaded photo) on each tote card"
    - "Totes with no photos show a placeholder icon instead of a thumbnail"
    - "Dashboard shows a 'Recent Totes' section with cover thumbnails"
    - "Uploading a photo to a tote on its detail page works end-to-end"
    - "Deleting a tote photo via lightbox works and gallery updates"
    - "After creating a tote, user is redirected to detail page where photo upload is immediately available"
  artifacts:
    - path: "src/app/totes/[id]/page.tsx"
      provides: "Tote detail page with photo gallery and upload"
      contains: "PhotoGallery"
    - path: "src/app/totes/page.tsx"
      provides: "Tote list page with cover thumbnails and post-creation redirect"
      contains: "cover_photo_id"
    - path: "src/app/api/dashboard/route.ts"
      provides: "Dashboard API with recent totes and cover photo IDs"
      contains: "recent_totes"
    - path: "src/app/page.tsx"
      provides: "Dashboard UI with Recent Totes section showing cover thumbnails"
      contains: "Recent Totes"
  key_links:
    - from: "src/app/totes/[id]/page.tsx"
      to: "src/components/photos/"
      via: "import { PhotoGallery, PhotoUpload }"
      pattern: "import.*PhotoGallery.*PhotoUpload"
    - from: "src/app/totes/[id]/page.tsx"
      to: "/api/totes/[id]/photos"
      via: "PhotoUpload POST and PhotoGallery fetch"
      pattern: "api/totes.*photos"
    - from: "src/app/totes/page.tsx"
      to: "/api/photos/[id]/thumbnail?source=tote"
      via: "img src for cover thumbnail"
      pattern: "api/photos.*thumbnail.*source=tote"
    - from: "src/app/totes/page.tsx"
      to: "/totes/[id]"
      via: "router.push after tote creation"
      pattern: "router\\.push.*totes/"
    - from: "src/app/page.tsx"
      to: "/api/dashboard"
      via: "fetch for recent_totes with cover_photo_id"
      pattern: "recent_totes"
---

<objective>
Wire shared photo components into the tote detail page and add cover thumbnails to the tote list page and dashboard. After tote creation, redirect to the detail page where photo upload is immediately available (per user decision: upload available on tote creation and on detail page). This completes the user-facing tote photo experience.

Purpose: Users can now photograph totes and see cover thumbnails everywhere totes appear -- completing the core feature gap.
Output: Tote detail page with photo gallery/upload, tote list with cover thumbnails and post-creation redirect, dashboard with Recent Totes section.
</objective>

<execution_context>
@/Users/kenmoini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kenmoini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-tote-photos/02-RESEARCH.md
@.planning/phases/02-tote-photos/02-01-SUMMARY.md
@.planning/phases/02-tote-photos/02-02-SUMMARY.md

<interfaces>
<!-- Components and APIs available from Plans 01 and 02 -->

From src/components/photos/index.ts (created in Plan 02):
```typescript
export { PhotoGallery } from './PhotoGallery';
export { PhotoUpload } from './PhotoUpload';
export { PhotoDeleteConfirm } from './PhotoDeleteConfirm';
```

Component props:
```typescript
interface PhotoUploadProps {
  entityType: 'item' | 'tote';
  entityId: string | number;
  currentPhotoCount: number;
  maxPhotos: number;
  onUploadComplete: () => void;
  maxUploadSize: number;
}

interface PhotoGalleryProps {
  photos: PhotoRecord[];
  entityName: string;
  source: 'item' | 'tote';
  onPhotoDeleted: () => void;
}
```

From src/types/index.ts (updated in Plan 01):
```typescript
export interface TotePhoto {
  id: number;
  tote_id: string;
  filename: string;
  original_path: string;
  thumbnail_path: string;
  file_size: number;
  mime_type: string;
  created_at: string;
}

export interface DashboardData {
  total_totes: number;
  total_items: number;
  recent_items: (Item & { tote_name: string })[];
  recent_totes: (Tote & { item_count: number; cover_photo_id: number | null })[];
}
```

API contracts from Plan 01:
- GET /api/totes/:id/photos -> { data: TotePhoto[] } (ordered by created_at ASC)
- GET /api/totes -> each tote has cover_photo_id field
- Cover thumbnail URL: /api/photos/{cover_photo_id}/thumbnail?source=tote
- Full-size photo URL: /api/photos/{photo_id}?source=tote

From src/app/totes/[id]/page.tsx (1121 lines):
- Fetches tote data from /api/totes/:id (includes items with photos)
- Has a fetchTote function (useCallback) that reloads tote data
- Displays items list with thumbnails
- Has settings fetch for max_upload_size
- Need to: fetch tote photos separately from /api/totes/:id/photos, add PhotoGallery + PhotoUpload above items list

From src/app/totes/page.tsx (716 lines):
- Fetches totes from /api/totes
- Renders tote cards in a grid
- Each card shows tote name, location, item count, etc.
- handleCreateTote function: creates tote via POST /api/totes, shows success toast, closes modal, refetches list
- After creation, newTote.id is available -- currently stays on list page
- Need to: render cover_photo_id as thumbnail, redirect to detail page after creation

From src/app/api/dashboard/route.ts (40 lines):
- Returns { total_totes, total_items, recent_items }
- Need to add: recent_totes query with cover_photo_id

From src/app/page.tsx (174 lines):
- Dashboard page consuming /api/dashboard
- Shows metric cards + recent items list
- Need to add: Recent Totes section below metrics, above or alongside recent items
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add photo gallery and upload to tote detail page, redirect after creation</name>
  <files>src/app/totes/[id]/page.tsx, src/app/totes/page.tsx</files>
  <action>
**Part A: Tote detail page photo support (`src/app/totes/[id]/page.tsx`)**

**1. Add imports:**
- `import { PhotoGallery, PhotoUpload } from '@/components/photos';`
- `import { TotePhoto } from '@/types';` (if not already imported via PhotoRecord)

**2. Add tote photos state and fetch:**
- Add state: `const [totePhotos, setTotePhotos] = useState<TotePhoto[]>([]);`
- Add fetch function (useCallback): `fetchTotePhotos` -- GET `/api/totes/${toteId}/photos`, set photos from response.data
- Call `fetchTotePhotos` in the existing useEffect alongside the tote data fetch
- The `fetchTotePhotos` function will also be passed as the `onUploadComplete` and `onPhotoDeleted` callback to the components

**3. Add max upload size:** If the page doesn't already fetch settings for max_upload_size, add a settings fetch. Check the existing code -- if it already has `maxUploadSize` from settings (as the item detail page does), reuse that. If not, add: fetch `/api/settings`, extract `max_upload_size` value, default to 5242880.

**4. Add photo UI above items list:**
Position the photo section ABOVE the items list (per user decision: "Gallery positioned above the items list on tote detail page"). Add a section with:
- A "Photos" heading (or integrate into an existing section header)
- `<PhotoGallery photos={totePhotos} entityName={tote.name} source="tote" onPhotoDeleted={fetchTotePhotos} />`
- `<PhotoUpload entityType="tote" entityId={toteId} currentPhotoCount={totePhotos.length} maxPhotos={3} onUploadComplete={fetchTotePhotos} maxUploadSize={maxUploadSize} />`
- If totePhotos is empty and not uploading, show a subtle placeholder message like "No photos yet" with a camera icon (Camera from lucide-react)

**Part B: Post-creation redirect (`src/app/totes/page.tsx`)**

**5. Add router import and redirect after tote creation:**
Per user decision: "Upload available on tote creation and on detail page." The create tote modal cannot include inline photo upload (no tote ID exists until creation succeeds). Instead, after successful tote creation, redirect the user to the new tote's detail page where photo upload is immediately available.

- Add `import { useRouter } from 'next/navigation';` if not already present
- Add `const router = useRouter();` in the component
- In the `handleCreateTote` function, after `const newTote = json.data;`:
  - Show the success toast: `showToast(\`Tote "${newTote.name}" created! Redirecting to add photos...\`, 'success');`
  - Reset form and close modal as before
  - Replace the `await fetchTotes()` call with `router.push(\`/totes/${newTote.id}\`);` to redirect to the detail page
  - This makes photo upload available as part of the tote creation flow per the locked decision, while respecting the architectural constraint that a tote ID is needed for photo upload

**Do NOT modify:** Items list rendering, item actions, tote editing form, breadcrumbs, or any other existing functionality on either page (aside from the creation redirect).
  </action>
  <verify>
    <automated>cd /Users/kenmoini/Development/tote-sonar && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
- Tote detail page fetches and displays tote photos using PhotoGallery component
- PhotoUpload component available on tote detail page for uploading photos
- Gallery positioned above items list per user decision
- Upload, viewing, lightbox navigation, and delete all work for tote photos
- Empty state shows placeholder with camera icon
- After creating a tote from the list page modal, user is redirected to the new tote's detail page where photo upload is immediately available
- Toast message indicates redirect for adding photos
- Existing tote detail and list functionality unchanged
- TypeScript compiles
  </done>
</task>

<task type="auto">
  <name>Task 2: Add cover thumbnails to tote list page and dashboard</name>
  <files>src/app/totes/page.tsx, src/app/api/dashboard/route.ts, src/app/page.tsx</files>
  <action>
**1. Update `src/app/totes/page.tsx`** (716 lines) -- Add cover thumbnails to tote cards:
- The totes list API (Plan 01 Task 3) now returns `cover_photo_id` for each tote.
- In the tote card rendering, before the tote name/details, add a cover thumbnail area:
  - If `tote.cover_photo_id` exists: render `<img src={/api/photos/${tote.cover_photo_id}/thumbnail?source=tote} alt="${tote.name} cover" />` with appropriate Tailwind classes (rounded corners, object-cover, consistent size like 80x80 or similar to match item thumbnails in the app).
  - If no cover photo: render a placeholder div with a subtle icon (Package or Camera from lucide-react, using muted colors like text-gray-300).
- Per user decision: "Totes with no photos show a placeholder icon (subtle camera or package icon)."
- Style to match existing item thumbnail patterns in the app. Use existing CSS classes if available, or create similar Tailwind utility classes.

**2. Update `src/app/api/dashboard/route.ts`** -- Add recent totes with cover photos:
- Add a query for recent totes (5 most recent by created_at DESC):
```sql
SELECT t.*, COUNT(i.id) as item_count,
  (SELECT tp.id FROM tote_photos tp WHERE tp.tote_id = t.id ORDER BY tp.created_at ASC LIMIT 1) as cover_photo_id
FROM totes t
LEFT JOIN items i ON i.tote_id = t.id
GROUP BY t.id
ORDER BY t.created_at DESC
LIMIT 5
```
- Add `recent_totes` to the response data object.
- The DashboardData type was already updated in Plan 01 to include `recent_totes`.

**3. Update `src/app/page.tsx`** (174 lines) -- Add Recent Totes section to dashboard:
- The dashboard currently shows metric cards (total totes, total items) + "Recently Added Items" list.
- Add a "Recent Totes" section. Per research recommendation: place it below the metric cards and above "Recently Added Items" (or side by side if layout allows).
- Render recent totes as a list or card grid (match the style of the recent items section):
  - Each tote entry shows: cover thumbnail (or placeholder icon), tote name (as link to `/totes/${tote.id}`), location, item count.
  - Cover thumbnail: if `tote.cover_photo_id`, render img with `/api/photos/${tote.cover_photo_id}/thumbnail?source=tote`; else render placeholder icon.
- Section header: "Recent Totes" with a link to "/totes" ("View all").
- If no totes exist, show a brief empty state message.

**Do NOT modify:** Metric cards logic, recent items section, or any existing dashboard functionality.
  </action>
  <verify>
    <automated>cd /Users/kenmoini/Development/tote-sonar && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
- Tote list page shows cover thumbnail on each tote card (or placeholder icon for totes without photos)
- Dashboard API returns recent_totes with cover_photo_id
- Dashboard UI shows "Recent Totes" section with cover thumbnails
- Totes without photos display placeholder icon consistently across list and dashboard
- All existing list page and dashboard functionality unchanged
- TypeScript compiles
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete tote photo feature end-to-end</name>
  <files>none</files>
  <action>
Human verifies the complete tote photo feature across all three plans. All automation is complete -- this is visual and functional verification only.

Start the app with `npm run dev` and test the following scenarios at http://localhost:3000.
  </action>
  <verify>
1. Start the app: `npm run dev`, open http://localhost:3000

2. **Dashboard** -- Verify "Recent Totes" section appears (initially with placeholder icons since no tote photos yet)

3. **Tote list page** (/totes) -- Verify tote cards show placeholder icons where cover photos will go

4. **Create tote and upload photos** -- Click "Create Tote", fill in the form, submit:
   - Verify success toast mentions redirecting to add photos
   - Verify you are redirected to the new tote's detail page
   - Verify photo section appears above items list with "No photos yet" placeholder
   - Upload a photo using drag-and-drop (drag an image file onto the drop zone)
   - Upload a second photo using the browse button
   - Verify both photos appear in the gallery grid

5. **Gallery lightbox** -- Click a photo thumbnail:
   - Verify full-size image opens in lightbox overlay
   - Verify left/right navigation arrows work (if 2+ photos)
   - Verify keyboard: ArrowLeft/Right navigate, Escape closes
   - Verify photo counter shows "1 of 2"

6. **Cover thumbnails** -- Navigate back to:
   - Tote list (/totes): Verify the tote now shows its first uploaded photo as cover thumbnail
   - Dashboard (/): Verify "Recent Totes" shows the tote with cover thumbnail

7. **Delete photo** -- Go back to tote detail:
   - Click a photo, click the delete (trash) icon in lightbox
   - Confirm deletion
   - Verify photo removed from gallery
   - Verify cover thumbnail updates on list/dashboard if the first photo was deleted

8. **Item photos still work** -- Navigate to any item detail page:
   - Verify photo upload, gallery, lightbox, and delete still work (no regressions)
   - Verify item photo lightbox has same navigation arrows

9. **Max 3 photos** -- Try uploading a 4th photo to a tote:
   - Verify upload is disabled or returns error when 3 photos exist
  </verify>
  <done>Human confirms all tote photo features work correctly with no regressions in item photo functionality. Type "approved" or describe issues.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit`
2. Tote detail page shows photo gallery + upload above items list
3. After creating a tote, user is redirected to detail page where photo upload is immediately available
4. Tote list page shows cover thumbnails (first uploaded photo) on tote cards
5. Dashboard shows "Recent Totes" section with cover thumbnails
6. Placeholder icons shown for totes without photos
7. All photo operations work for totes (upload, view, lightbox nav, delete)
8. Item photo functionality has zero regressions
</verification>

<success_criteria>
- Users can upload up to 3 photos per tote and see them in gallery on detail page
- Upload is available as part of tote creation flow via redirect to detail page (per locked decision)
- Tote list and dashboard show first uploaded photo as cover thumbnail
- Lightbox has left/right navigation arrows and keyboard support
- Placeholder icon shown for totes without photos
- Shared photo components reused between items and totes (no duplicated photo UI code)
- Human verification passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-tote-photos/02-03-SUMMARY.md`
</output>
